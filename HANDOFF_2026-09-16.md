# 인계 — SEO/GEO 세션 (2026-09-15 저녁 ~ 09-16 새벽)

> 대표 마지막 말: *"뭘 했다는건지 뭘 하면 좋아진다는건지 답답하다. 과제 넣고 어떻게 해야 할지
> 문제를 남겨서 다음 핸드오프에서 해결하게 해."*
> ⇒ 이 파일은 **한 줄 요약 → 무엇이 좋아지나 → 남은 과제 → 원자료** 순서다.

---

## 한 줄

**"봇이 우리 페이지에 닿지도 못하고 있었고, 제품 8개가 서로 남남인 회사로 선언돼 있었다."**
둘 다 고쳐서 배포했다. 그런데 **아직 검색에 안 잡히는 상태 자체는 안 풀렸다** — 그건
며칠 뒤 색인으로만 확인된다.

## 무엇을 하면 뭐가 좋아지나 (이걸 먼저 읽어라)

| 한 일 | 이게 없으면 | 이제 기대되는 것 | 언제 확인 |
|---|---|---|---|
| 고아 페이지 해소 (3사이트) | 구글이 sitemap 으로 **발견만** 하고 크롤 안 함 → 색인 0 | 크롤 → 색인 → 노출 | **2026-10-13** GSC |
| 회사 정체 통합 (7사이트) | 제품 8개가 **남남 회사 8개**로 읽힘. 한 곳이 만든다는 사실이 기계에 없음 | 제품 하나가 뜨면 나머지가 권위를 나눠 받음 | 즉시 반영, 효과는 느림 |
| 측정기(자) 교체 | **틀린 숫자로 엉뚱한 곳을 고침** (실제로 그럴 뻔했다) | 매주 같은 자로 비교 가능 | 이미 확인 |
| 실험 원장 자동 리마인드 | ⏳ 가 쌓이고 확인일이 조용히 지나감 (8/31 이후 실제로 그랬다) | 판정일에 슬랙으로 뜸 | 9/25 첫 발화 |

🔴 **핵심: 오늘 한 건 전부 「닿게 만들기」다. 「잘 보이게」가 아니다.**
브랜드명 완전일치 검색(`"the-moment.us"`)에서도 우리가 **0건**인 상태는 그대로다.

---

## 🔴 남은 과제 (다음 세션이 할 일 — 순서대로)

### 과제 1. 브랜드 한 줄 확정 — **대표가 고르면 그날 끝난다**

지금 네 군데가 다 다른 말을 한다 (실측):

| 자리 | 현재 문구 |
|---|---|
| `<title>` | 일하는 사람을 위한 도구를 만듭니다 |
| 홈 `h1` | 모멘터스 — 작게 만들어 빨리 내놓는 제품들 |
| `/about/` `h1` | 나머지는 저희가 합니다 |
| `llms.txt` | 강형모 1인 AI 스튜디오. 쓸모 있는 것만 만듭니다 |

**답은 이미 대표가 써놨다.** `~/Library/Mobile Documents/iCloud~md~obsidian/Documents/sexyflash/wiki/decisions/Reci-4차-재정의-능력이-아니라-결과-진짜-문제는-못-내보내는-것.md` (2026-08-05):

> **WHY** — "능력을 나눠주는 시대가 끝났다." 도구·빌더·대시보드·튜토리얼은 전부 능력을
> 나눠주는 형태고, 받은 사람은 여전히 배우고·판단하고·실행해야 한다. 그래서 도구는
> 언제나 소수만 성공시킨다.
> **WHAT** — **"할 수 있게" 해주지 않는다. "이미 되어 있게" 한다. 남기는 건 취향 하나.**

같은 폴더 `선택지를-주는-것은-판단을-되돌려주는-것이다.md` (2026-07-31):
> 도구의 말투 "50장 뽑았어요, 고르세요" ↔ **담당자의 말투 "이걸로 하겠습니다. 이유는 이겁니다. 진행할까요?"**

**제품 12개 전수 대조 — 12/12 가 맞는다** (억지로 끼운 게 아니다):
빈방=취소표 새로고침 / 퀵팡=상품 열어보기 / ChatPage=긴 영상 보기 / 마크=시안 백 장 넘기기 /
더플랜=플래너 직접 만들기 / 헤이레시=촬영 준비 / 큐=예상질문 뽑기 / 컨텍스트=매번 상황 설명 /
팀AI=먼저 말 걸기 / 플리퍼=화면 만져 넘기기 / her=타이핑 / 인기순정렬=뭐가 잘됐나 뒤지기

#### 후보 (대표가 하나 고르면 된다)

**A. "대신 해놓는 곳."**
부제: *기다리고 찾고 정리하는 일은 기계가 합니다. 남는 건 고르는 일 하나입니다.*
- 장점: "거긴 뭐 하는 곳이야?" 에 한 문장으로 답이 된다. 제품 12개가 전부 여기 들어간다.
  새 제품이 이 문장에 안 맞으면 우리 제품이 아니다 — **필터로도 쓸 수 있다.**
- 단점: 대행사(사람이 대신 해주는 곳)로 오해될 여지.

**B. "이미 되어 있게."**
부제: *할 수 있게 가르치지 않습니다. 해놓고 드립니다. 남기는 건 취향 하나.*
- 장점: 대표 원문에 가장 가깝다. 경쟁 제품(전부 "할 수 있게" 해준다)과 **정확히 반대말**이라
  대비가 선다.
- 단점: 한국어로 소리 내 읽으면 어색. 첫 대면에서 무슨 뜻인지 바로 안 온다.

**C. "나머지는 저희가 합니다."** (이미 `/about/` 에 있는 문장)
- 장점: **이미 쓰고 있다.** 사람 말투고 따뜻하다. 검증된 문장.
- 단점: "나머지"가 무엇의 나머지인지 문장 혼자서는 모른다. 단독으로는 약하다.

**D. 대표가 처음 말한 "1인 AI 빌더 대한민국 대표" 류 — 🚫 비권장.** 근거 3개(아래 「죽은 가정」).

**추천: A.** 부제로 대표 원문(B)을, `/about/` 은 C 로 유지. 셋이 같은 말이라 안 싸운다.

#### 문장이 정해지면 박을 자리 (한 번에 전부, 글자까지 같게)
- `~/Projects/momentus/scripts/gen_site.py` — `TITLE`/`h1`/`ORG["description"]`/`llms.txt` 생성부
- 제품 8개 푸터(공용 셸) — `momentus/scripts/sync_shell.py` 로 전파
- 🚫 **한정어 없이 "모멘터스" 단독으로 쓰지 마라** (이름 충돌, 아래 함정 참조)

---

### 과제 2. `sameAs` 가 비었다 — **스키마로 못 채운다. 채널을 만들어야 한다**

오늘 실측(직접 긁음): 한컴·Atlassian·이스트소프트·티맥스·컬리·크림 **6/6 전부** `sameAs` 를 쓴다.
우리는 **Play 개발자 페이지 1개**뿐이다. 동명 5개와 우리를 가르는 **제일 센 신호**인데 제일 비었다.

- 대표 결정 필요: 어떤 채널을 모멘터스 명의로 열 것인가
- ⚠️ 위키 `2026-04-23_brand_a1-personal-front.md` 에 "강형모 개인 얼굴 앞" 결정이 **decided 로
  남아 있는데 대표가 2026-09-15 에 뒤집었다** (*"만든 사람은 아무도 관심 없어, 모멘터스가
  뭐 하는 곳인지를 기억했으면"*). 이미 7/9 `CONCEPT_LOCK.md` 에 *"제품이 주인공, 나는 뒤"* 로
  드리프트가 있었다. **그 결정문에 폐기 기록을 남겨야 한다** — 안 그러면 다음 세션이 또
  개인 브랜딩으로 돌아간다. (이번 세션에서 안 했음 ⚠️미확인 → 할 일)

---

### 과제 3. 색인 확인 — **오늘 한 일이 실제로 먹었는지**

- **2026-09-16(오늘)**: 빙 BWT 데이터가 들어올 예정 (9/14 등록 +48h).
  `node ~/Projects/momentus/observatory/recheck.mjs` — 매일 09:30 크론이 자동으로 돌고
  판정이 나면 `~/slack-bot/memory/secretary/inbox.md` 에 붙는다. 🟡(대기)면 조용하다.
  ChatGPT 웹검색이 빙 인덱스를 타므로 **GEO 의 실질 관문일 가능성이 높다.**
- **2026-09-21(월) 08:00**: 관측소 주간 크론 **첫 실전 발화**. 지금까지 한 번도 안 떴다
  (원장 W38 행은 9/14 수동 회차). 이날 표가 슬랙에 안 오면 크론을 의심해라.
- **2026-09-25 / 2026-10-13**: 실험 판정일. 판정 대기 표는
  `~/Projects/momentus/docs/SEO_EXPERIMENTS.md` 의 「⏳ 판정 대기」, 확인은
  `python3 ~/Projects/momentus/scripts/exp_due.py`.

---

### 과제 4. kontext 배포 — **코드는 됐고 배포만 안 했다**

- 커밋 `5a8ddd3` (`~/Projects/kontext`), `npm run build` **rc=0 확인**.
- 🚫 **배포 안 함.** Vercel 이고 **유일한 유료 구독 제품**이라 대표 확인 뒤에 올린다.
- ⚠️ `node_modules` 가 통째로 비어 있어서(0개) `npm install` 을 새로 돌렸다. 476개 설치됨.
  lockfile 변경 여부 ⚠️미확인.

### 과제 5. 헤이레시 2건 (대표가 "일단 넘어가" 라고 한 것)

- `/apps/*` **25장이 고아**. `/apps/all` 이 `'use client'` 라 목록이 HTML 에 안 실린다.
  목록 확보됨: hairstyle-tryon, body-profile, age-shift, life4cuts, pet-and-me, bucket-list-shot,
  achievement-shot, insta-stage, reci-uniform-studio, fashion-lookbook, plating-master,
  lifestyle-shot, image-studio, menu-shot, restaurant-scene, seasonal-menu, blur-fixer,
  horizon-fix, out-focus, photo-restorer, photo-colorizer, product-lighting, portrait-lighting,
  flat-to-3d, sticker-pack
- 엔티티 스키마도 아직 `Organization "heyreci"` (legalName 에만 모멘터스).
- 저장소: `/Volumes/Seagate Backup/Projects/Reci` (**외장하드**, 마운트 확인 필요)

---

## 🔴 죽은 가정 (다시 살리지 마라 — 전부 실측으로 죽었다)

1. **"1인 AI 스튜디오 / 1인 AI 빌더 대한민국 대표" 포지션** — ❌
   - 자동완성 실측: `1인 AI` → 창업·기업·스타트업·크리에이터. **검색자는 창업 희망자지 우리 고객이 아니다.** 네이버는 자동완성 자체가 없다(수요 0).
   - `AI 스튜디오` 는 **구글 AI Studio 가 점령**. 포지션 단어로 최악.
   - 실제 검색 결과: 요한스튜디오(코칭 판매)·DoAllJ·솔로프리너 컨퍼런스·피터 레벨스 번역 뉴스레터. **그 자리는 "1인 창업 하는 법"을 파는 시장이다.**
   - GEO 원장 선택기준 **557개 전수**: `1인`·`제작자`·`만든 사람`·`창업` **0회**.
     실제 기준은 **무료 21회 · 한국어 26회 · 국내 10회** + 구체 기능.
2. **`disambiguatingDescription` 에 "누구누구와는 무관하다"** — ❌ 넣었다가 대표 지적으로 제거.
   실측 0/8 곳이 안 쓴다. 엔티티 연결은 **같이 등장하는 것끼리 엮인다** — 부정문을 써도
   "같이 나왔다"만 남는다. 게다가 구조화데이터는 **어필하는 자리**다.
3. **GEO 측정기의 `criteria` 를 우리 페이지에 심기** — ❌ 3주 내내 원장이 내놓던 할 일인데,
   회차 파일을 열어보니 그 `criteria` 는 **경쟁사가 뽑힌 이유** 목록이었다.
   heyreci 질문 실측: `카페24 앱스토어 입점 · 무료 체험 · 상품 URL만 넣으면 자동 생성 ·
   촬영 로봇 · 이미 카페24 쓰는 분` — **「촬영 로봇」은 스튜디오랩 하드웨어다. 우리에게 없다.**
   인용은 전부 경쟁사 자기 도메인(draph.art, store.cafe24.com). **우리가 그 자리에 페이지가
   없어서 안 뽑힌 것이지 문장이 모자라서가 아니다.**
   ⇒ `~/slack-bot/geo_desk.py` 가 criteria 를 그대로 할 일로 내놓는 것 자체를 고쳐야 한다.

---

## 함정 (이번에 데인 것)

- 🔴 **"모멘터스" 이름은 이미 5개가 쓴다.** Momentus Inc.(나스닥 MNTS, 우주) · 삼소나이트
  Momentus 여행가방 · **모먼트어스 momentus.kr(한국 안경, 한국어 표기까지 같다)** ·
  bemoment.us · 영단어 momentous. **이름 단독으로 승부 보지 마라. 항상 한정어와 붙여 쓴다.**
- 🔴 **브랜드명 완전일치 검색에서 the-moment.us 가 0건이다.** `site:` 로도 안 나온다.
  구글엔 28일 노출 5회뿐. **색인이 사실상 없는 상태다.**
- 🔴 **빌드를 두 개 겹쳐 돌리지 마라.** mark 에서 그렇게 해서 `dist/.prerender` 가 서로
  지워져 빌드가 깨졌다(RC=1). mark 풀빌드는 **20분**이다. 하나만 돌리고 기다려라.
- 🔴 **탭·칩(`<button>`)은 봇에게 길이 아니다.** 오늘 고친 3사이트가 전부 같은 병이었다.
  사람용 탭은 두고 **진짜 `<a>` 를 눈에 보이게** 따로 놓는다. 🚫 숨기지 마라.
- 🔴 **같은 걸 두 벌 적으면 한쪽만 고쳐지고 다른 쪽이 조용히 샌다.**
  apex Organization 이 정확히 그랬다 — 리치 블록을 써놓고 후처리가 덮어써서
  **라이브엔 네 줄만 나가고 있었다.**
- ⚠️ Cloudflare 캐시 때문에 배포 직후 첫 조회가 **옛 내용**일 수 있다. 한 번 더 확인해라.
- ⚠️ 네이버 웹 검색 스크랩은 **403**. geo_probe 의 naver_web 경로를 쓰거나 포기해라.

---

## 원자료 (도구 출력 그대로)

### 배포된 것 — 바깥에 나갔다 (되돌리려면 재배포)

| 사이트 | 배포 | 라이브 확인 |
|---|---|---|
| the-moment.us | `wrangler deploy` 4회 (마지막 `473d458c`) | Organization 필드 20개 · 제품 8 · 도구 6 |
| teamai.the-moment.us | Pages 2회 (마지막 `fefdf865`) | 고아 0/145 · Organization=모멘터스 |
| mark.the-moment.us | Pages 2회 (마지막 `c68dbe62`) | 고아 0/867 · Organization=모멘터스 · `/explore/` 업종 22개 |
| cue.the-moment.us | `npm run deploy` (`cf1642ce`) | Organization=모멘터스 |
| bb.the-moment.us | Pages (`c577c3d9`) | JSON-LD 3노드 |
| notes.the-moment.us | `wrangler deploy` (`59fa75cf`) | Organization @id 통일 |
| kontext.the-moment.us | **배포 안 함** | — |

### 커밋 (이번 세션)

    momentus  aa7f608 23f6b14 ffc1d79 0088f1e ede44b8 11f740d 8fc4007 e2ce34e 3748fe1 9bfce62 7f7bad3
    cue       4a14c49
    mark      555a312 acefc66
    teamai    2e18f757 eb644f71
    notes     701cf39
    binbang   f05df19c
    kontext   5a8ddd3  (배포 안 함)

전부 `main`, 미푸시. `~/slack-bot/auto_deploy.py` 가 매시 :20 에 자동 push 한다.

### 고아율 — 조치 전 → 후 (전부 배포 후 재측정)

    팀AI      141/144 = 98%  →  0/145 = 0%   ✅
    마크      224/867 = 26%  →  0/867 = 0%   ✅
    모멘터스    4/33  = 12%  →  0/32  = 0%   ✅
    헤이레시    25/112 = 22%  →  (미조치)
    cue         0/132 = 0%   (원래 정상)
    노트        3/49  = 6%   · kontext 1/8 · 빈방 0/1

### 자(尺)를 간 사고 — `scripts/seo_check.py check_orphans`

옛 구현은 "sitemap 의 얕은 경로 앞 40장"을 허브로 삼아 거기 걸린 링크만 셌다. **양방향으로 틀렸다:**
- 과대 — mark 는 경로가 전부 `insights/<글>` 이라 *기사*가 허브 자리를 먹었다. **99.2% 는 허상**(실제 26%).
- 과소 — teamai 를 **40% 로 보고**했는데 실제 98%. 조용해서 안 볼 뻔했다.
- 한글 경로 퍼센트 디코딩 누락 → 한글 URL 이 전부 고아로 보였다.
지금은 **홈에서 BFS**(얕은 곳 먼저, sitemap 밖 허브도 넘어간다). 예산 기본 = sitemap×2(최소 120·상한 1200),
소진되면 본문이 "과대집계일 수 있다"를 같이 적는다. `~/bin/seo_check.py` 사본도 동기화했고
**시스템 파이썬 3.9 로 import·실행 확인**(크론 경로).

### AI 리퍼러 — 90일 전수, 단 1건

    2026-09-07 · chatgpt.com · 모바일 · /tools/quickpang/ · 1세션 1조회
    claude.ai · perplexity.ai = 0
    ⚠️ 헤이레시의 claude.the-moment.us 11세션은 **우리 웹 콘솔(ttyd)** 이다. AI 아니다.
    조회 스크립트: node ~/Projects/cue/scripts/ga_ai.mjs 90

→ ChatGPT 가 집은 건 **제품 랜딩이 아니라 무료 도구 페이지**였다. GEO 를 걸고 있던 7개 제품
   중 어느 것도 아니다. 🚫 n=1 로 규칙 만들지 마라. 다음에 확인할 **가설**로만 적어 둔다:
   「제품 소개형보다 불편 하나를 해결하는 도구형이 AI 답변에 먼저 실린다」

### GSC 숫자 두 가지 (혼동 주의)

같은 속성 `sc-domain:heyreci.com`, 같은 28일: 필터 없음 **노출 1**(질의 단위) vs
`--host heyreci.com` **노출 10**(페이지 단위). 관측소는 **페이지 단위**를 쓴다.
🚫 두 값을 나란히 놓고 "늘었다"고 하지 마라. 클릭은 어느 쪽이든 **0**.

### 업계 Organization 필드 실측 (오늘 직접 긁음)

    한글과컴퓨터   18  (identifier·companyRegistration·knowsAbout·award 까지)
    Atlassian      13  (numberOfEmployees·knowsAbout)
    이스트소프트     8
    티맥스소프트     7
    마켓컬리         5
    크림             4
    무신사·토스·네이버·앤트로픽·라쿠텐·사이보우즈 — Organization JSON-LD 아예 없음
    disambiguatingDescription 쓴 곳: 0/8 · sameAs 쓴 곳: 6/6

### 이번에 만든/고친 파일 (절대경로)

    ~/Projects/momentus/scripts/seo_check.py          (고아 판정 BFS 로 교체 + --budget)
    ~/Projects/momentus/scripts/exp_due.py            (신규 — 확인일 지난 실험만 고른다)
    ~/Projects/momentus/scripts/gen_site.py           (ORG 단일화 · 태그 실링크 · sitemap 301 제거 · support 링크)
    ~/Projects/momentus/observatory/notify.py         (월요일 표 밑에 판정 대기 목록)
    ~/Projects/momentus/docs/SEO_EXPERIMENTS.md       (⏳ 판정 대기 표 + 2026-09-15 회차)
    ~/bin/seo_check.py                                (사본 동기화)
    ~/Projects/cue/public/momentus-org.js             (신규)
    ~/Projects/cue/scripts/ga_ai.mjs                  (신규 — AI 리퍼러 낱건)
    ~/Projects/cue/{scripts/sync-seo.mjs,src/pages2.mjs,src/nowpage.mjs,src/index.js,public/landing.html}
    ~/Projects/mark/src/pages/explore.astro           (업종 허브 22개 실링크)
    ~/Projects/mark/src/layouts/Base.astro            (Organization → WebSite + 모멘터스)
    ~/Projects/mark/src/pages/insights/[slug].astro · src/pages/[kw]/[slug].astro
    ~/Projects/teamai/site/app/crew/page.tsx          (신규 — 크루 139명 전체 명부)
    ~/Projects/teamai/site/{app/sitemap.ts,components/LandingHero.tsx,app/globals.css,app/page.tsx}
    ~/Projects/binbang/site/index.html
    ~/Projects/notes/web/src/seo.js
    ~/Projects/kontext/src/app/layout.tsx             (배포 안 함)

### 테스트

- ⚠️ **회귀 테스트 안 돌렸다.** `~/slack-bot/tests/` 는 이번 변경과 무관하지만 확인 안 함.
- mark `npm run build` rc=0 + `seo-audit 통과` · teamai `npm run build` rc=0 · kontext rc=0
- `exp_due.py` 양방향 확인: 오늘 0건 / 9-25 기준 8건 / 10-12 기준 10건
