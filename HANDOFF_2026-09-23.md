# 인계 — 2026-09-23 (SEO/GEO 전면 진단 + 구글·네이버 실측)

> 이 세션은 mark 의 cover_v 드롭 버그 조사로 시작해 **전사 SEO/GEO 상태 진단**으로 번졌다.
> 결론이 방향을 바꿨으므로 **아래 §5(결론) 를 먼저 읽어라.**

---

## 0. 한 줄

구글은 우리 제품 사이트를 사실상 모른다(구조 문제, 위생 작업으로 안 풀림). 네이버는
유입이 오는데 **전부 롱테일**이고, 네이버 블로그 824편은 거기 기여가 **0** 이다.
다음 수는 글을 더 쓰는 게 아니라 **이미 있는 콘텐츠에 검색어 손잡이를 붙이는 것**.

---

## 1. 바닥 확인 (도구 출력 그대로)

### 저장소별 상태 (2026-09-23 확인)

    mark      main  미푸시 7건 (f3d4f87 · 555a312 · acefc66 포함 = 이전 세션 것)
              b5f9931 fix(pipeline): cover_v1·cover_v2 시안 드롭
              922141b fix(SEO): minPostsPerTag 배선 + 3
              ab0dc3c docs(SEO): Plan B 43일 연체
              ca1a41e docs(SEO): Plan B 판정 ❌
              작업트리 509건 변경(대부분 재생성된 work/journal md)

    momentus  main  미푸시 1건
              522c02d docs: 네이버 유입은 100% 롱테일
              a74bd5e docs: kontext·빈방 공유 카드 복구 기록
              eee1ddb feat(관측소): GSC 속성 분리 + K2 판정
              ae73c4f docs(SEO_EXPERIMENTS): mark Plan B 를 K2 로 편입
              caa20c2 docs: 대조군 heyreci.com

    cue       main  미푸시 0  (0b24604 feat(gsc): 속성 목록 8개)
              ⚠️ 7a0699b · 764ffa5 · f73298e 는 **이 세션이 만든 것이 아니다**
                 (나이틀리/다른 세션). f73298e 에 "사이트맵 재제출 2건" 이 들어있다

    kontext   main  미푸시 0  (2256671 · 2779c0d 푸시 완료 → Vercel 배포됨)
    binbang   main  미푸시 0  (e670ea83 푸시 완료 + wrangler 배포됨)
    slack-bot main  미푸시 0  (이 세션은 slack-bot 코드를 건드리지 않았다)

### 테스트 (실제로 돌림, 2026-09-23)

    mark/pipeline/test_slot_of.py           all ok   ← 이 세션에서 신설
    mark/pipeline/test_slugify.py           all ok
    mark/pipeline/test_slack_block_limit.py all ok
    momentus/scripts/exp_due.py             출력 없음 = 확인일 지난 실험 0건

⚠️미확인: slack-bot 전체 스위트는 안 돌렸다. kontext/binbang 은 테스트 스위트를 안 돌렸다
(kontext 는 `npm run build` 통과를 확인했다).

---

## 2. 바깥에 나간 것 (되돌리기 어려운 순서)

| 무엇 | 상태 |
|---|---|
| **GSC 도메인 속성 5개 신설** — mark·notes·teamai·kontext·bb | ✅ 생성·소유확인 완료(하위도메인은 apex 가 확인돼 있으면 **자동 통과**, DNS 추가 0건) |
| **서비스계정 권한 부여** — `gindex@sexyflash-mcp-2026.iam.gserviceaccount.com` 을 위 5개 속성에 '전체' 사용자로 추가 | ✅ `sites.list` 9개 전부 보임 |
| **사이트맵 재제출** — notes·teamai·kontext·bb (자기 속성에서 PUT 204) | ✅ kontext·bb 즉시 읽힘 / notes·teamai 여전히 isPending |
| **kontext 배포** (Vercel, git push 로) | ✅ 라이브 반영 확인 |
| **빈방 배포** (`wrangler pages deploy site --project-name binbang-samples`) | ✅ 라이브 반영 확인 |
| **디스콰이엇 제품 등록 3건** — mark·cue·heyreci | ⏳ **승인 대기중** (disquiet.io/products/{mark,cue,heyreci}) |
| **네이버 로그인** (대표가 직접) + 세션 스냅샷 | ✅ `~/.config/naver-session/the-momentus/storage_state.json` |

없앤 것·삭제한 것 **없음**. 결제·발송 **없음**.

⚠️ 디스콰이엇 계정은 **대표의 구글 SSO** 로 로그인돼 있다(크롬 9222). 승인 결과는
그 계정 알림으로 온다.

---

## 3. 이번에 고친 것 (코드)

### mark

- `pipeline/assemble_r2.py` — `slot_of()` 가 확장자(.png/.jpg/…)와 **개정 접미사 `_v\d+`**
  를 정규화. `cover_v1.png` 이 커버로 인정 안 돼 **참여작 15건이 조용히 드롭**되던 버그
  (09-13·19·21·22 네 번 보고, git 로그상 한 번도 고쳐진 적 없음). 실측 복구 **작업물 +9**.
  `keys_by_slot()` 은 같은 슬롯에 여러 판이 오면 번호 높은 판을 남긴다.
  **`_prev_best()` 대표작 고정이 핵심이다** — 이게 없으면 점수가 바뀌어 대표작이 갈아타는데
  자산 다운로드가 resume-safe 라 `assets/work/<cid>/hero.webp` 에 **옛 이미지가 그대로 남아**
  매니페스트와 화면이 어긋난다. 실행 결과 대표작 rid 변경 0건 확인.
- `pipeline/test_slot_of.py` — 신설. **옛 코드로 되돌리면 첫 단언에서 실패하는 것까지 확인**.
- `src/config/seo.ts` — `minPostsPerTag` 2 → 3. **이 상수는 선언만 돼 있고 아무도 안 읽고
  있었다**(판정 숫자 `2` 가 `[tag].astro` 와 `astro.config.mjs` 에 복제). 양쪽을 단일 소스로 배선.
  결과: 사이트맵 899 → **859**(나이틀리 배포 후 라이브 실측).
- `scripts/seo-audit.mjs` — 한도 95% 초과 시 **경고**(실패 아님) + 요약에 여유 칸 +
  **CF Pages 파일 한도(20,000) 검사**. 종전엔 침묵 아니면 빌드 사망 둘뿐이었다.
- `docs/SEO.md` §4 — Plan B 판정 ❌ 기록. §4.5 — 천장 상황.

### kontext

- `src/app/layout.tsx` — `alternates: { canonical: "./" }`(Next 가 경로마다 풀어줌) +
  og:image/twitter:images → `https://the-moment.us/og/kontext.png`.
- `public/sitemap.xml` — `/blog`(308) → `/insights`.
- **FAIL 25 → 0.** `npm run build` 통과 + 로컬 `next start` 로 경로별 canonical 확인 후 배포.

### binbang

- `site/index.html` — canonical + og 8종 + twitter 4종. 이미지는 apex `/og/binbang.png`.
- **FAIL 8 → 0 · warn 0.**

### momentus

- `observatory/sites.json` — `gsc` 를 **사이트마다 자기 속성**으로. (종전엔 6개가
  `sc-domain:the-moment.us` 하나를 공유해서 "노출 5회가 누구 것인지" 가를 수 없었다.)
- `cue/scripts/gsc.mjs` — SITES 8개로.
- `docs/SEO_EXPERIMENTS.md` — K2 편입·판정, S1~S6, 그리고 **롱테일 결론**(2026-09-22 회차).

---

## 4. 실측 숫자 (다음 세션이 근거로 쓸 것)

### 구글 (URL 검사 API, 우산 속성과 각자 속성 **양쪽에서 같은 결과**)

| 사이트 | 상태 | 마지막 크롤 |
|---|---|---|
| the-moment.us (apex) | ✅ 색인됨 | 2026-08-27 |
| heyreci.com (**대조군, 별도 apex**) | ✅ 색인됨 (`/pricing` 도) | 2026-09-08 |
| cue | 크롤링됨–미색인 | 2026-09-08 |
| mark | 크롤링됨–미색인 | **2026-06-20** |
| notes · teamai · kontext · bb | **Google 에 알려지지 않음** | 없음 |
| apex `/products/mark/` | **알려지지 않음** | 없음 |

GSC 노출(28일): heyreci 11 · mark 4 · apex 4 · **나머지 전부 0**.
빙 색인: the-moment.us 987 · cue 546 · mark 386 · heyreci 247 · kontext 17.
→ **빙은 2,100장 이상을 색인했다. 기술적으로 멀쩡하다는 증명이다. 구글만 안 받는다.**

배선은 다 확인했다 — 사이트맵 200·유효 XML, **Googlebot UA 로도 200**(엣지 차단 아님),
robots `Allow: /` + `Sitemap:`, **apex 홈이 6개 하위도메인에 평범한 `<a href>`**(JS·nofollow 아님).
«막혀서»가 아니라 **«안 와서»** 다.

### 네이버

- 블로그(the-momentus) **824편** = 인사이트 104 + 시안 720(의료 69·카페 51·기업 89·기타 511)
- 조회수 15일 합계 **63**(하루 4.2), 09-21 **0**. 피이웃 0 · 서로이웃 0 · 기타 100%
- 크리에이터 어드바이저 **유입 검색어 = 데이터 없음(검색 유입 0명)**
- 색인은 ✅ (글 제목 그대로 검색 시 3/3 노출)
- 실수요 키워드 **0/6** (카페 로고 제작 · 로고 디자인 비용 · 병원 로고 디자인 ·
  분식집 로고 비용 · 로고 제작 · 치과 로고 디자인) — 그 자리는 전부 사람 디자이너 블로그
- 헤드 키워드로 **사이트도 0/6** (모의면접 · AI 모의면접 · 면접 연습 · 로고 만들기 ·
  무료 로고 · 로고 시안)

### GA4 네이버 유입 랜딩 페이지 (28일, 2026-08-25~09-22) — **이게 핵심 근거다**

    cue 128세션:  / 18 · /playbook/final-interview-recontact 16 · /prep/gongmuwon 14
                  /company/coupang 12 · /company/woowahan 10 · /job/krafton-8511513002 10
                  /playbook/late-for-interview 10 · /job/coupang-8006899 8
                  /playbook/reapply-after-rejection 8 · /company/toss/data 7
    mark 16세션:  /find/ 3 · / 2 · /제조-로고/work-211122-rv-8fd0e855/ 2 · 나머지 1씩 산발
    momentus 13:  /tools/insta-rank/ 5 · /products/flipper/setup/ 4 · /tools/chatpage/ 2

### 나이틀리 싱크 (2026-09-23 04:54, 신규 5건) 이후 현재

- 사이트맵 **859 / 900** (여유 41)
- dist **19,707 / 20,000** (여유 293 ≈ 작업물 32건분) ← **이제 파일 한도가 앞선 벽이다**

---

## 5. 결론 (방향)

1. **구글**: 하위도메인 구조 + 백링크 0 때문이다. `*.the-moment.us` 는 구글 눈에 각각
   **남남인 신생 사이트**다. 대조군 heyreci.com(별도 apex)은 같은 스택인데 색인됐다.
   사이트 *안에서* 예산을 재분배하는 조치(noindex·사이트맵 다이어트·canonical)는 전부
   «크롤러가 온다»는 전제 위에 있고 **그 전제가 거짓**이다. → 위생 작업은 여기서 끊는다.
2. **네이버 유입은 100% 롱테일**이다. cue 가 mark 의 8배인 이유는 **검색창에 실제로 치는
   말과 1:1 로 붙은 페이지**(회사명·공고번호·구체적 상황)를 수백 개 갖고 있어서다.
3. **mark 에 손잡이가 없는 건 우연이 아니다.** `anonymize.py` 가 의뢰인 상호를 전부
   지웠다 — 라우드 원본과 중복을 피하려면 **꼭 필요했던** 조치다. 그 대가로 검색에 걸릴
   고유명사를 같이 잃었고, 남은 제목이 「신뢰감 있는 조합형 약국 로고」 라 아무도 그렇게
   검색하지 않는다. 저널 116편도 에세이체 제목이라 같은 병이다.
4. **네이버 블로그 824편은 채널로서 죽어 있다**(하루 4명·검색 유입 0). 시안 자동발행이
   매일 쌓이는 중이고 주제 희석 위험이 있다. **중단 여부는 대표 결정 대기 중.**

---

## 6. 대표가 한 말 (판단 기준이 된 것, 원문)

- *"아니 그러니깐 뭘하라고? 어디에 누가 언급을 해주겠어? 아니 어떻게해야ㅐ해? 뭔소리야?"*
  → 추상적 조언 금지. 구체적 사이트 이름·클릭 순서까지.
- *"크롬을 띄워서 니가 알아서 다 입력하고 하면 되잖아 로그인이 필요하다면 그것만 내가
  해주면 되는거 아니야?"* → 브라우저 작업은 직접 한다. 로그인만 요청.
- *"mark 저널 116편을 네이버 블로그에 이미 올리고 있잖아 그래서 거기서 트래픽이 오고
  있잖아 무슨 소리야?"* → **대표가 맞았고 내가 틀렸다**(§7 참조).
- *"아니 너무 답답한 게 지금 한 3개월 이거 붙잡고 있는데 해결되는 방도가 없어"*

---

## 7. 해봤는데 아니었던 것 / 내가 틀렸던 것 (반복 금지)

- ❌ **"네이버 블로그에 저널을 안 올리고 있다"** — 완전히 틀렸다. `slack-bot/naver_blog.sqlite`
  만 보고 "6/18 이후 0건"이라 했는데, 그건 **슬랙봇 수동 RPC 기록**일 뿐이다.
  실제 발행은 **nightwork 의 `tasks/blog_pipeline.py` → `scripts/naver_publish.py`(인사이트)
  · `scripts/sian_naver.py`(시안)** 이 한다. 824편이 이미 올라가 있다.
- ❌ **RSS 최신 50건으로 판단** — 시안 발행이 몰리면 저널이 밀려 안 보인다. 카테고리별
  글 수로 세라(`blog.naver.com/PostList.naver?blogId=the-momentus&categoryNo=4~8`).
- ❌ **네이버 순위를 로그인된 크롬에서 측정** — 페이지에 자기 블로그 링크(`MyBlog.naver`)가
  섞여 **전부 "노출됨" 오탐**이 났다. 반드시 **로그아웃 브라우저(9222)** 로 재라.
- ❌ **toolify.ai / theresanaiforthat.com 무료 등록** — 지금은 **유료**다($99 / $49~1,000).
  무료라고 추천했던 건 틀렸다. 디스콰이엇만 무료.
- ❌ **Site Verification API 로 GSC 속성 자동 생성** — GCP 프로젝트(119460573186)에서
  API 가 비활성이고 서비스계정에 `serviceusage.services.enable` 권한이 없다. **브라우저로
  Search Console UI 를 쓰는 게 정답이었다**(하위도메인은 소유확인 자동 통과라 오히려 빠르다).
- ❌ **`naver_auth.check()` 를 함부로 돌리지 마라** — 프로필로 새 브라우저를 띄우면
  9234 상주 인스턴스와 겹쳐 **쿠키가 통째로 날아간다**(2026-09-02 실사고). CDP 로 붙어서
  쿠키를 직접 읽어라.

---

## 8. 열려 있는 것 / 깨져 있는 것

- 🟠 **mark 미푸시 7건.** 그중 3건(f3d4f87·555a312·acefc66)은 이전 세션 것이다.
- 🟠 **mark 작업트리에 남의 미커밋 수정** — `pipeline/anonymize.py` · `pipeline/gen_content.py`
  · `pipeline/test_slugify.py` · `src/components/Footer.astro` · `src/styles/global.css`.
  오늘 새벽 나이틀리 세션이 고쳤고 **"지시가 없어서 커밋하지 않았습니다"** 라고 로그에 남겼다
  (`~/Library/Logs/Nightwork/moment-sync.log`). 커밋 여부 결정 필요.
- 🔴 **notes · teamai 사이트맵이 재제출 후에도 `isPending`** — 구글이 안 읽는다.
  확인일 **2026-10-06**(S4).
- 🟠 **dist 19,707 / 20,000** — 여유 293(작업물 ~32건분). 넘으면 `wrangler pages deploy` 실패.
  사이트맵(859/900, 여유 41)보다 이쪽이 먼저 터진다.
- ⏳ 디스콰이엇 3건 승인 대기.
- ❓ **대표 미결정 2건**: ① 네이버 블로그 시안 자동발행을 멈출까?
  ② GitHub 저장소 하나를 public 으로 열까(README 백링크용)?

---

## 9. 후속 (같은 날, 다음 세션)

- ✅ mark 저널 79편 제목을 네이버 실수요 검색어로 교체 — md + D1 `blog_posts`. 근거·되돌리기는
  `mark/docs/naver-keywords/`. 실험 N1(확인일 10-21) 등록.
- ✅ 대표 결정 ① **시안 자동발행 중단** — nightwork 가동본(외장하드)의 `tasks/blog_pipeline.py`
  + `scripts/sian_naver.py`, 스위치 `SIAN_NAVER_PUBLISH=1` 로 재개. 커밋 4e99f5a
  (브랜치 feature/winning-conventions, 미푸시 — 다른 세션 작업 브랜치라 손대지 않음).
- ❌ 대표 결정 ② **GitHub public 전환 기각** — "절대 안 되지". 다시 제안하지 말 것.
- ✅ mark·momentus 푸시 완료. mark 작업트리의 나이틀리 수정 5개 파일 커밋(테스트 3종 통과).
