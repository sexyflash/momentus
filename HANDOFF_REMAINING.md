# 핸드오프 — 모멘터스 웹 남은 일 (2026-08-25)

> 인사이트 정비 5단계는 끝났다(보고서: shelf-mx7.pages.dev/d/insights-unify-20260825/).
> 이 문서는 **그 뒤에 남은 것 전부**다. 항목마다 *지금 상태 · 왜 남았나 · 하는 법 · 안 해도 되는 이유*를
> 적었다. 급한 순서가 아니라 **결정이 필요한 것 → 손만 대면 되는 것 → 안 해도 되는 것** 순이다.
>
> 큐 글 표지 30장은 분량이 커서 따로 뺐다 → `~/Projects/cue/HANDOFF_COVERS.md`

---

## A. 결정 완료 (둘 다 보류 — 나중에 함께 판단)

### A-1. 헤이레시에 패밀리바를 달 것인가 — ✅ 패밀리바 **안 단다** / 돌아올 링크는 ⏸ **보류 (8/25 오후)**

> **8/25 오후 재지시**: *"컨텍스트, 헤이레시는 달지 말자. 나중에 하자."*
> → 아래 규격은 **확정된 채로 대기**한다. 지금 넣지 마라. 나중에 할 때 그대로 쓰면 된다.
> Reci 세션에도 보류 전달 완료(미배포 상태에서 멈춤 — `heyreci.com` 링크 0개 실측).

**대표 결정**: *"헤이레시에는 패밀리바 달지 마. 근데 모멘터스로 올 수는 있어야 할 텐데 그 정도 GNB 장치만 있으면 어떨까? 작게."*

**왜 필요했나 (실측 8/25)**: `heyreci.com` 에 `id="mmt-bar"` 0개는 물론이고 **`the-moment.us` 로 가는
링크 자체가 0개**였다. 푸터의 `MOMENTUS (모멘터스) · 대표자: 박진이 …` 는 사업자 표기일 뿐 링크가 아니다.
즉 본진→헤이레시는 되는데 헤이레시→본진이 끊긴 **편도**였다.

**규격 (Reci 저장소에서 구현. 이 저장소 아님)**

| | 내용 |
|---|---|
| 헤더 | 우측 끝, 로그인·시작하기 버튼보다 **왼쪽**에 작은 텍스트 링크 `◆ MOMENTUS` 하나. 13px, 흐린 회색(`--text-tertiary`), 호버 때만 진해짐 |
| 새 탭 | `target="_blank" rel="noopener"`. 셀러가 이미지 생성 중에 잘못 눌러 작업이 날아가는 게 더 큰 손해다 |
| 모바일 | 헤더에선 숨김 — 좁은 화면에 끼우면 CTA를 밀어낸다 |
| 푸터 | **줄을 새로 만들지 마라.** 이미 있는 `MOMENTUS (모멘터스)` 그 글자에만 링크를 건다 |
| 대상 | `https://the-moment.us/` 홈. 제품 목록이 아니라 "만든 곳"을 보여주는 장치다 |

🚫 **`sync_shell.py` 의 `TARGETS` 에 heyreci 를 넣지 마라.** 넣는 순간 56px 공용 바 + 토큰 전체가
딸려 온다 — 그게 곧 패밀리바를 다는 것이고, 방금 안 달기로 한 그것이다. Reci 저장소에
**하드코딩 링크 1개**로 끝낸다. 주소가 안 바뀌니 갈릴 것도, 동기화할 것도 없다.

⚠️ **부수효과 — 자동으로 따라오게 두지 마라.** `gen_site.py` 의 판정 기준은
*"눌렀을 때 돌아올 길(패밀리 바)이 있느냐"*(2026-08-24 대표 정리)인데, 이 링크가 붙으면 그 기준이
형식상 뒤집힌다. 그래도 **헤이레시를 본진 바 앞줄로 꺼내지 마라** — 앞줄 기준은 원래
"패밀리 바를 달고 있는 사이트"이고 헤이레시는 여전히 안 단다. 앞줄 승격은 별개 판단이다.

### A-2. 컨텍스트에 패밀리바를 달 것인가 — ✅ **결정됨 (8/25): 안 단다. 나중에**

**지금**: 없다. 영문 사이트(Features/Pricing/Docs)에 베타 딱지가 붙어 있다.

**왜 남았나**: 한글 바(제품 전체·인사이트·소개)를 영문 사이트 위에 얹으면 어색하다.
영문 바를 따로 만들면 그때부터 바가 두 벌이 된다 — 이 문서 전체가 경계하는 그 상태다.

**✅ 결정됨 (8/25 오후): 달지 않는다. 나중에 판단.** *"컨텍스트, 헤이레시는 달지 말자. 나중에 하자."*
돌아올 링크(A-1 규격)도 같이 보류다. 컨텍스트가 정식 출시하고 한글을 지원할 때 둘을 함께 판단한다.

---

## B. 손만 대면 되는 것

> ⚠️ **8/25 재정리.** 종전 B는 4건이었는데 그중 3건이 "안 해도 되는 이유"로 끝났다.
> 그럼 그건 손댈 일이 아니라 **안 하기로 한 것**이거나 **조건 대기**다. 그대로 두면 다음 세션이
> B를 열 때마다 4건을 처음부터 다시 검토한다. 실측해서 옮겼다 — 지금 B에 남은 건 **1건뿐**이다.

### B-1. 죽은 CSS 두 덩어리 삭제 + `.gnb` 한 줄 — ✅ **완료 (8/25)**

빌드가 "중복 클래스 4건"이라고 경고하지만 **실측하면 등급이 다르다.** 4건을 다 만지지 마라.

| 클래스 | 실측 (gen_site.py) | 할 것 |
|---|---|---|
| `.vc-thumb--g` | 521 / 846 — 같은 셀렉터 3개가 **값만 다르게** 두 번. 846이 이김 | 🔴 **521 블록 삭제** (죽은 코드) |
| `.fl-card` | 1793 / 1853 — 마크업은 2976행 `<span class="fr"><img>` **한 형태뿐**. 1793은 `.fl-card img` 직속을 겨냥한 **옛 마크업용** | 🔴 **1793 블록 삭제** (죽은 코드) |
| `.gnb` | 87 / 102 — 중복이 아니라 정의가 두 덩어리로 쪼개진 것. 단 102의 `padding:0 20px` 이 87의 `padding:0 var(--gut)` 을 덮는다 | 🟡 **그 한 줄만 정리.** 지금 87의 padding을 고치면 안 먹는다 |
| `.ap-qa` | 875 / 976 — 875는 `max-width:none` 한 줄뿐이고 976엔 max-width가 없다 | ⚪ **충돌 아님. 그대로 둬라** |

**왜 하나**: 지금은 뒤엣것이 이겨서 화면이 멀쩡하다. 하지만 **앞엣것을 고치는 사람은 "왜 안 바뀌지"**
로 시간을 태운다. 가드를 만든 이유가 그거다.

**가드도 같이 좁혔다.** 종전 가드는 "같은 선택자가 2곳"이면 전부 울려서, 프로퍼티가 하나도
안 겹치는 **의도된 분할**(`.gnb` 크롬 층 여백 · `.ap-qa` max-width)까지 매 빌드마다 경고했다.
상시로 켜진 경고는 신호가 아니라 배경 소음이고, 진짜 충돌이 섞여도 안 보인다.
→ **같은 프로퍼티가 겹칠 때만** 울리고 어느 프로퍼티인지 찍는다. 값이 같아도 울린다(앞을 고치면
여전히 안 먹으니 함정은 같다). 순수 문자열 파싱이라 룰 #1 무관.

**결과**: 경고 4건 → **0건**. 산출 CSS 는 렌더링상 동일(9줄 삭제·4줄 이동, 죽은 선언만 빠짐).

⚠️ **작업 중 발견 — 통째로 지웠으면 사고였다.** `.fl-card` 앞 블록에 `b`·`i` 규칙이 있는데
뒷 블록엔 **없다**. 마크업(`gen_site.py` 카드 생성부)이 `<b>{제목}</b><i>{설명}</i>` 를 쓰므로
앞 블록을 통째로 삭제하면 플리퍼 카드 캡션이 무너진다. **뒷 블록으로 옮겼다.**
중복 정리는 "뒤엣것이 이기니 앞엣것을 지운다"가 아니라 **프로퍼티 단위로 대조**해야 한다.

**양방향 검증 완료**: 진짜 충돌 · 값까지 같은 재선언 · 의도된 분할 · `@media` 재정의 ·
값에 콜론이 든 경우(`url(data:...;base64,)`) 5종을 실제로 돌려 잡을 것만 잡는 것을 확인했다.

---

## B'. 조건이 오면 하는 것 (지금은 아니다)

### 컨텍스트 RSS — **글이 5편을 넘으면** 그때

**지금**: `kontext.the-moment.us` 의 `/rss.xml`·`/feed.xml` **둘 다 404**(8/25 실측).
그래서 본진은 글 3편을 `data/products.json` 의 `posts` 배열에 **손으로 적어** 쓰고 있다.

**안전망은 이미 있다**: 빌드할 때마다 컨텍스트 목록 페이지를 세어 개수가 다르면 경고가 뜬다
(`gen_site.py` 의 `posts_index`). 조용히 낡지는 않는다.

**하는 법**: 컨텍스트에 `/rss.xml` 라우트 추가(`src/data/blog/posts.json` 이 이미 있어 5분짜리다)
→ `products.json` 의 kontext 항목에서 `posts`·`posts_index` 를 지우고 `feed` 를 채운다.

**왜 지금이 아닌가**: 글이 3편이라 손목록이 안 무겁다. D의 **5편 규칙**과 같은 조건이니 따로 세지 마라.

---

## C. 안 하기로 한 것 (재론 방지)

| 항목 | 판단 | 근거 |
|---|---|---|
| 무료 도구 카드의 서비스 로고 6종 | **그대로 둔다** | 대표: *"서비스 로고 쪽은 신경 쓰지 마"*(8/25) |
| 컨텍스트 배너 해상도(유튜브 1280×720 천장) | **그대로 둔다** | 대표: *"그냥 냅둬"*(8/25) |
| 큐 목록 카드를 본진 규격으로 | **안 맞춘다** | 큐 목록은 글 카드가 아니라 상황별(`준비 중`·`면접 후`) 필터가 붙은 다른 물건. 억지로 맞추면 쓰기 나빠진다 |
| 플래너 인사이트를 메뉴에 걸기 | **글 5편 전엔 안 건다** | 1편짜리 메뉴는 "관리 안 하는 사이트"라고 광고하는 것 |
| 큐 글 주소 `/playbook/` → `/insights/` | **안 옮긴다** | 31편이 이미 색인. 방문자에겐 차이 0인데 301 사슬만 는다 |
| 플리퍼 옛 `/apps/` 301 정리 | **안 지운다** | `/apps/flipper/` → `/products/flipper/` **301 정상 동작 확인**(8/25). 301은 비용 0이고 지우면 옛 외부 링크·검색결과가 죽는다. (⚠️ 언젠가 지운다면 `apps/<slug>/` 산출물이 남아 있지 않은지 먼저 봐라 — 파일이 남으면 리다이렉트를 이긴다) |
| 플리퍼 설정 스크린샷 2장 다시 찍기 | **안 찍는다** | `hisense-4-flipper-detail.png` 아래가 비어 보이는 건 **잘린 게 아니라 원래 그런 설정 화면**이고 맨 아래는 시스템 내비바다. 내용 전달에 문제 없다 |

---

## D. 계속 지킬 규칙 (8/25 합의)

- **글 5편 미만이면 메뉴에 걸지 않는다.** 페이지는 남기되 링크는 숨긴다. 새 제품도 동일.
- **목록 주소만 맞추고 글 주소는 옮기지 않는다.**
- **각 저장소에서 카드 CSS 를 다시 쓰지 않는다.** 쓰는 순간 다시 두 벌이 된다.
  모양의 단일 소스는 `gen_site.py` 의 `NEWS_CSS` 하나이고, `sync_shell.py` 가 밀어 넣는다.
- **공용 셸에 백틱(`` ` ``)이나 `${` 를 넣지 마라.** 플래너·큐는 셸을 JS 템플릿 리터럴 안에 담아서
  한 글자에 배포가 죽는다. `sync_shell.py` 가 이제 결정론으로 막지만, 걸리면 셸이 **안 나간다**.
- **2주 뒤(≈9/8) GA 숫자를 보고** 인사이트에 더 투자할지 정한다. 지금 새 블로그는 만들지 않는다.
  속성: `the-moment.us` · 측정 ID `G-1T66ZV28MB` (마크·큐 것과 섞지 마라).

---

## E. 검증 명령 (뭘 하든 마지막에)

```bash
cd ~/Projects/momentus && ~/slack-bot/.venv/bin/python - <<'EOF'
from playwright.sync_api import sync_playwright
import time
# (이름, URL, 글 카드 셀렉터 | None=인사이트 없는 사이트)
#   ⚠️ 사이트마다 카드 클래스가 다르다. .nws-card 만 세면 플래너(.pcard)·컨텍스트(Tailwind)가
#      글이 있는데도 0 으로 찍혀 "죽어도 지금과 같은 0" 이 된다 — 그래서 사이트별로 못 박는다.
T=[("본진","https://the-moment.us/insights/",".nws-card"),
   ("마크","https://mark.the-moment.us/insights/",".nws-card"),
   ("큐","https://cue.the-moment.us/insights",".dg-card"),
   ("플래너","https://notes.the-moment.us/insights/","a.pcard"),
   ("컨텍스트","https://kontext.the-moment.us/insights",'a[href^="/blog/"]'),
   ("빈방","https://bb.the-moment.us/",None)]
with sync_playwright() as pw:
    b=pw.chromium.launch(); p=b.new_context(viewport={'width':1440,'height':1000},
        extra_http_headers={'Cache-Control':'no-cache'}).new_page()
    bad=0
    for n,u,sel in T:
        try:
            r=p.goto(u+('?z=%d'%time.time()), wait_until='networkidle'); p.wait_for_timeout(1200)
            d=p.evaluate("""(sel)=>({bar:!!document.querySelector('#mmt-bar'),
              card: sel? document.querySelectorAll(sel).length : -1,
              ovf:document.documentElement.scrollWidth-document.documentElement.clientWidth,
              broken:[...document.querySelectorAll('img')].filter(i=>i.complete&&!i.naturalWidth).length})""", sel)
            c = "—" if d['card']<0 else str(d['card'])
            flag = ""
            if d['ovf']>0 or d['broken']>0 or r.status!=200: flag=" ← 확인"; bad+=1
            if sel and d['card']==0: flag=" ← 글 0건! 목록이 비었다"; bad+=1
            print(f"{n:6} {r.status} bar={str(d['bar']):5} card={c:>3} 넘침={d['ovf']} 깨짐={d['broken']}{flag}")
        except Exception as e:
            print(f"{n:6} ERROR {str(e)[:70]}"); bad+=1
    b.close()
    print(f"\n{'문제 없음' if not bad else f'확인 필요 {bad}건'}")
EOF
```

**⚠️ 확인은 반드시 `Cache-Control: no-cache` + 고유 쿼리로.** 클라우드플레어가 301 과 HTML 을
캐시해서, 고친 게 안 고쳐진 것처럼 보이는 사고가 이 작업에서만 세 번 났다.

**⚠️ 2026-08-25 고침 — 종전 스크립트는 세 사이트를 못 재고 있었다.** `.nws-card` 하나만 세서
**플래너(`a.pcard`)와 컨텍스트(Tailwind, 고정 클래스 없음)가 글이 있는데도 `card=0`** 으로 찍혔다.
즉 *그 목록이 통째로 죽어도 지금과 똑같은 0* 이라 **검사가 아니라 장식**이었다.
→ 사이트별 셀렉터를 표에 못 박고, 셀렉터가 있는데 0 이면 **"글 0건!"** 로 잡는다.
인사이트가 없는 사이트(빈방)는 `None` 으로 두어 `—` 로 찍는다 — 0 과 구분되어야 한다.
**새 사이트를 추가하면 그 사이트의 카드 셀렉터도 같이 넣어라.** 안 넣으면 조용히 0 이다.

**8/25 실측**: 본진 75 · 마크 96 · 큐 37 · 플래너 1 · 컨텍스트 3 · 빈방 — · 넘침 0 · 깨짐 0.
