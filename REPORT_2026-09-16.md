# 작업 완료 보고 — 2026-09-16

> **09:20 갱신** — 아침에 대표가 두 가지를 지시: ① 보류 2건 배포 ② 브랜드 문구 재수정.
> 둘 다 처리했다. 아래 §0 이 최신, §1~ 은 새벽분(문구는 §0 이 이깁니다).

---

## §0. 아침 처리분 (09:00~09:30)

### 브랜드 문구 재수정 — 배포 완료

대표 지적 둘:
- **「작은 도구」** — 제품을 작아 보이게 한다. *"그게 작은 도구라는 거랑은 좀 안 맞는 거 같아"*
- **「결정적인 순간」** — *"결정적인 순간인데도 말을 웃겨"* (뻣뻣하다)

대표가 그대로 불러 준 문장으로 갔습니다:

| 자리 | 지금 라이브 |
|---|---|
| 홈 `h1` | **순간순간을 소중하게.** |
| 그 아래 | 모멘터스(MOMENT·US)는 순간순간을 소중하게 하는 제품을 만듭니다.<br>취소표가 뜨는 10분, 면접장 문이 열리는 3초 — 그때 맨손이지 않게. |
| `<title>` | 모멘터스(MOMENTUS) — 순간순간을 소중하게 하는 제품 |
| `llms.txt`·JSON-LD | 같은 문장 + **이름은 MOMENT(순간)와 US(우리)에서 왔습니다** |
| `/about/` 배너 | 모멘터스 = **MOMENT**(순간) + **US**(우리) |
| 푸터 | 순간순간을 / 소중하게. |

**이름 뜻이 이제 슬로건 안에 삽니다.** 예전엔 로고 밑 영문(`the moment · us`)에만 있었습니다.
h1 이 혼자선 추상적이라(대표도 인정) **바로 밑에 구체 순간 두 개**를 붙였습니다.
`products.json` 의 `moment` 필드(14/14)와 `/about/` 「그 순간」 표는 그대로라 관문은 안 흔들립니다.

🚫 **「작은 도구」·「결정적인 순간」으로 되돌리지 마라** — 반나절 쓰고 뺀 말입니다.

### 배포 결과

| 대상 | 결과 |
|---|---|
| the-moment.us | ✅ `4159ef44` — 홈·소개·llms.txt 라이브 확인 |
| **kontext** | ✅ Vercel prod — JSON-LD 2노드(Organization `#org` + WebSite) **라이브 확인** |
| bb.the-moment.us (빈방) | ✅ Pages `604553df` — 이름 뜻 확인 |
| cue / reps | ✅ `516256c9` — 이름 뜻 확인 |
| mark | ⏳ 진행 중 (867페이지, 20분) |
| **notes** | ⏸ **일부러 뺐습니다** — 아래 |

**notes 를 안 올린 이유**: 다른 세션이 지금 그 저장소에서 작업 중입니다. 최신 커밋이
*"인수인계에 **미완 항목** 추가 — naver_sync 가 안 끝나 가격 판정이 아직 작동 안 한다"* 입니다.
지금 배포하면 **반쯤 된 기능이 그대로 나갑니다.** 그 세션이 끝난 뒤 그쪽에서 올리는 게 맞습니다.
(제 커밋 `60b1723` 은 그 안에 이미 들어가 있어 같이 나갑니다.)

### 이번에 드러난 것 — 공용 바가 안 닿는 사이트 3곳

인계표가 「제품 8개 푸터」라고 했는데 **실제로는 8곳이 아닙니다.** 실측:

| 공용 바 있음 | the-moment.us · mark · cue/reps · notes/pay · 빈방 |
|---|---|
| **공용 바 없음** | **teamai · kontext · heyreci** (`mmt-bar` 0건) |

이 셋은 각자 자기 셸을 씁니다. 브랜드 일관성에 구멍이고, 새 문구도 안 닿습니다.
**다음 할 일**로 남깁니다 — 공용 바를 넣을지, 각 셸에 문구만 박을지 결정이 필요합니다.

### 사고 하나 (기록)

mark 첫 빌드를 백그라운드로 돌렸는데 **중간에 죽어 `dist` 가 반쪽(1.2GB)으로 남았습니다.**
인계표가 경고한 바로 그 자리입니다. `dist` 를 지우고 처음부터 다시 돌렸습니다.
⚠️ **mark 빌드는 `&` 로 떼어 놓지 마라** — 세션이 정리될 때 같이 죽는다.

---

## §1~ 새벽분 (02:00~03:40)

> 아침에 이것만 읽으면 됩니다. 순서: **끝난 것 → 지금 바로 보실 것 → 남은 것 → 내가 정한 것**

---

## 한 줄

**브랜드 한 줄을 「작은 도구가 순간을 바꿉니다」로 확정해 the-moment.us 에 배포했고,
세션앱에서 선택지를 못 고르던 문제를 고쳤습니다.** 둘 다 라이브·실측 확인 끝났습니다.

---

## 1. 브랜드 — 확정·배포 완료 ✅

| 자리 | 지금 라이브에 있는 말 |
|---|---|
| 홈 `h1` | **작은 도구가 순간을 바꿉니다.** |
| 그 아래 | 모멘터스는 결정적인 순간에 쓰는 작은 도구만 만듭니다. |
| `<title>` | 모멘터스(MOMENTUS) — 결정적인 순간에 쓰는 작은 도구 |
| 로고 밑 (머리·푸터·제품 공용 바) | `the moment · us` |
| `/about/` `h1` | 하루를 가르는 건 짧게 열렸다 닫힙니다. |
| `llms.txt` | 슬로건 + 순간 문장 2줄 |
| JSON-LD `description` | 앞에 정체 한 문장, **뒤의 구체 제품명은 그대로** |

**어떻게 여기까지 왔나** — 후보를 세 번 냈고 앞의 둘은 대표님이 기각하셨습니다.
기각 사유가 같았습니다: *"딱 읽어도 뭐가 딱 잡혀야 할 거 아니야."*
「안 해도 되는 일을 없앱니다」·「그 순간, 우리가」는 전부 **기능·개념**이라 읽어도 뭘 파는
집인지 안 잡혔습니다. **「작은 도구」가 물건 이름**이라 그 자리에서 그림이 잡힙니다.

### 관문을 코드에 박았습니다 (이게 이번 작업의 알맹이)

`data/products.json` 에 제품마다 **`moment` 한 줄** — **14/14, 누락 0.**
`/about/` 의 「그 순간」 표가 이 필드에서 나옵니다(손으로 적는 목록이 아닙니다).

> 빈방=취소표가 뜨는 순간 · 큐=면접장 문이 열리는 순간 · 퀵팡=살까 말까 정하는 순간
> ChatPage=이 영상을 볼지 말지 정하는 순간 · her=말이 먼저 나오는 순간 · …

**새 제품이 어느 순간에 쓰이는지 한 줄로 못 쓰면 표에 자리가 없습니다.**
「우리 팀은 그것만 판다」가 말이 아니라 파일로 강제됩니다.

### 죽은 포지션 제거
- `llms.txt` 「강형모 1인 AI 스튜디오」 · 제품허브 「1인 AI 스튜디오가 직접」 → 삭제.
  **라이브 잔존 0건 확인.**
- 위키 `decisions/2026-04-23_brand_a1-personal-front.md` → `status: superseded` +
  폐기 배너(뒤집은 근거 3건 + 「되살리지 마라」). 다음 세션이 개인 브랜딩으로 못 돌아갑니다.

### 커밋
- momentus `e6462e2` (브랜드 본체) · `1a0…` (대문 부제 줄바꿈)
- 배포 `wrangler deploy` → Version `96aed436` · **홈·소개·llms.txt·제품 8페이지 라이브 확인**

---

## 2. 세션앱(ccmobile) — 고침 완료 ✅  커밋 `0142136`

새벽 2시에 못 쓰셨던 그 판입니다. **추측하지 않고 47칸 tmux 에 진짜 클로드코드를 띄워
같은 판을 재현**했더니 네 가지가 한꺼번에 나왔습니다.

| 증상 | 진짜 원인 | 고침 |
|---|---|---|
| 「B. 이미 되어」— **이름이 두 동강** | 예시 상자가 있는 판은 CLI 가 **설명을 아예 안 그린다**(47·132칸 둘 다 실측). 앱이 접힌 이름 꼬리를 설명으로 읽어 앞부분만 남기고 **잘랐다** | 예시 판에선 이어지는 줄을 이름으로 붙임 |
| **2번을 고를 방법이 없음** | 「예시」 알약이 손가락보다 작아 빗나가면 그대로 제출. 숫자는 고르고 넘어가기까지 해 **되돌릴 수 없다** | **첫 누름=보기, 같은 줄 한 번 더=정하기.** 빗나가도 잃는 게 없음 |
| 예시가 **낱말 하나씩 세로로** | 47칸에서 예시 상자가 **11칸**으로 찌그러짐. 화면만 보고는 못 되살림 | 부서졌다고 **밝히고** 「예시 넓게 보기」. 답하면 저절로 원래 배치로 |
| **키보드 안 먹음** | 판에 걸린 키가 Esc 하나뿐 | ↑↓·숫자·엔터 추가 |

**「직접 타이핑이 사라졌다」도 제가 만든 것**이었습니다 — 보기만 되게 했더니
「Type something」 줄이 안 열렸습니다. 두 번 누르기로 되살렸습니다.

만드는 도중 **두 번 더 데었고 둘 다 고쳤습니다**: ① 칸이 혼자면 `resize-pane` 이 조용히
실패 → 창 넓히기로 넘어감 ② 넓힌 직후 다시 그리는 틈을 「닫혔다」로 보고 **0.5초 만에
도로 좁히고 있었음** → 유예 4초.

검증: 회귀 **19종 추가 · 488 전부 초록 · 되돌리면 실제로 빨간불 뜨는 것까지 확인.**
폰 폭(430px) 실측으로 전 구간 확인 — 47칸 낱말세로 → 넓히기 → 정상 → 취소 후 47칸 복귀.

---

## 3. 빙 BWT (인계 과제 3) — 판정 완료

돌렸습니다. **대조군 포함 전부 0** → 미리 정한 규칙대로 *「계기를 의심한다, 하루 더 기다린다」*.
**「GEO 가 안 된다」는 결론이 아닙니다.** 오늘(9/16) 한 번 더 보고 그래도 0이면
BWT UI 를 눈으로 확인해야 합니다(API 만의 문제일 수 있음).

---

## 4. 남은 것 — **승인이 필요한 둘**

### ① 제품 8곳 배포 (notes·cue·mark·binbang)
공용 바 워드마크에 `the moment · us` 붙이는 **화장 변경**입니다. 커밋은 끝났고 배포만 안 했습니다.
- 급하지 않습니다. **mark 풀빌드가 20분**이고 겹쳐 돌리면 `dist` 가 깨집니다 — 하나씩.
- 커밋: notes `60b1723` · cue `6d2f35b` · mark `f3d4f87` · binbang `978b0ab0`

### ② kontext 배포
커밋 `5a8ddd3` 까지 되어 있고 `npm run build` rc=0. **Vercel 이고 유일한 유료 구독 제품**이라
임의로 안 올렸습니다. (앞 세션부터 대기 중)

### ③ 헤이레시 `/apps/*` 고아 25장
대표님이 「일단 넘어가」 하신 건. 저장소가 **외장하드**(`/Volumes/Seagate Backup/Projects/Reci`)라
마운트 확인이 먼저입니다.

---

## 5. 내가 정한 것 (뒤집으실 수 있게 적어 둡니다)

- **제품 사이트 푸터(전자상거래 표기)엔 브랜드 문구를 안 넣었습니다.** PG 심사가 pay 와
  대조하는 블록이라 새벽에 건드리지 않았습니다. 넣고 싶으시면 한 줄이면 됩니다.
- **`/about/` 의 「저녁 7시엔 일이 아니라 사람이」 배너는 그대로 뒀습니다.** 새 문장과
  오히려 잘 맞습니다 — *"모멘터스라는 이름은 그 순간에서 왔습니다"* 가 이미 거기 있었습니다.
- **JSON-LD 는 앞 한 문장만 바꿨습니다.** 뒤의 구체 제품명 5개는 기계가 읽을 사실이라
  손대면 손해입니다.
- **notes·cue·mark 의 다른 미커밋 변경은 안 건드렸습니다** — 다른 세션 작업으로 보입니다.

---

## 6. 상태 (03:40 기준)

```
momentus   추적변경 0 · 미푸시 13   ← auto_deploy 가 매시 :20 자동 push
ccmobile   추적변경 0 · 미푸시 3
binbang    추적변경 0 · 미푸시 2
notes/cue/mark  내 작업 잔여 0 (남은 변경은 다른 세션 것)
ccmobile 서버 running · 시험용 tmux(probe2) 정리됨
```
